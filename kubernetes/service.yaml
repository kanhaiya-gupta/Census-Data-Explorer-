apiVersion: v1
kind: Service
metadata:
  name: database-service
spec:
  selector:
    app: database
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: transform-service
spec:
  selector:
    app: transform
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: load-service
spec:
  selector:
    app: load
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000